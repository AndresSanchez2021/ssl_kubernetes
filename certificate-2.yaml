apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: hello-v1
  namespace: default
spec:
  dnsNames:
    - unalmeet.page
  secretName: unal1-tls
  issuerRef:
    name: letsencrypt-cluster-issuer
    kind: ClusterIssuer